# 交叉注意力融合配方

## 掩码规则

| 注意力 | 掩码 |
|:------|:-----|
| 自注意力 | 因果下三角 |
| 交叉注意力 | 无（图像全可见） |

## 块结构

```
x = x + self_attn(ln1(x))
x = x + cross_attn(ln2(x), memory)
x = x + ffn(ln3(x))
```

## KV 缓存

图像键值计算一次，解码时直接复用。
