# 视觉编码器图块配方

## 图块投影

Conv2d(in=3, out=768, kernel=16, stride=16) → (B, 768, 14, 14) → (B, 196, 768)

## 2D 正弦位置

```
PE(row, col, 2i)   = sin(row / 10000^(2i/d))
PE(row, col, d+2i) = sin(col / 10000^(2i/d))
```

## 组件参数

| 组件 | 参数 |
|:----|:----|
| Conv2d | ~590K |
| 正弦位置 | 0 (固定) |
| CLS | 768 |
