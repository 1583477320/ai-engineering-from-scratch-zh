---
name: prompt-sam3-guide
description: SAM3 开放词汇分割选型与提示词设计指南
phase: 4
lesson: 24
tags: [sam3, open-vocab, segmentation, model-selection, prompt-engineering]
---

# SAM3 开放词汇分割选型与提示词设计指南

## 用途

本指南帮助开发者在以下场景做出正确决策：

1. **模型选型**：在 SAM3、Grounded SAM 2、YOLO-World、SAM-MI 之间选择
2. **提示词设计**：编写 SAM3 能高效处理的概念提示词
3. **流水线集成**：将开放词汇分割接入生产管线

---

## 第一部分：模型选型决策树

```
输入：任务描述
  │
  ├── 只需要边界框，不需要掩码？
  │   ├── 是 → 延迟 ≤ 50ms 吗？
  │   │   ├── 是 → YOLO-World 或 OV-DINO（实时开放词汇检测器，30-60fps）
  │   │   └── 否 → Grounding DINO + 后处理
  │   └── 否 → 需要视频跟踪？
  │       ├── 是 → 跟踪 10+ 实例？
  │       │   ├── 是 → SAM3.1 Object Multiplex
  │       │   └── 否 → SAM3 或 SAM2+Tracking
  │       └── 否 → 概念复杂度高？
  │           ├── 是（组合描述如「条纹红伞」）→ SAM3
  │           └── 否（单一名词如「校车」）→ 检查许可证约束
  │               ├── 需要宽松许可证？→ Grounded SAM 2（Apache）
  │               └── 无限制 → SAM3
  │
  许可证约束与部署环境：
  ├── 边缘设备（GPU 受限）→ SAM-MI 或 MobileSAM + 轻量检测器
  ├── 浏览器环境 → YOLO-World ONNX + MobileSAM ONNX
  └── 云端 GPU 集群 → SAM3（FP16，如延迟允许则 BF16）
```

---

## 第二部分：提示词设计规则

### 核心原则

| 规则 | 好写法 | 不好写法 |
|---|---|---|
| 短名词短语，不要完整句子 | `"猫"` | `"有一只猫在图片里"` |
| 具体名词 | `"滑板"` | `"骑的东西"` |
| 修饰语紧贴名词前 | `"红色汽车"` | `"汽车是红色的"` |
| 小写输入 | `"黄色校车"` | `"黄色校车"`（一致即可） |
| 单复数均可 | `"狗"` 或 `"狗群"` | 视实例数量预期选择 |

### 概念拆分

SAM3 每次前向传播只处理一个概念。多概念查询需要拆分：

```
用户输入: "猫, 狗和气球"
拆分结果: ["猫", "狗", "气球"]
处理方式: 循环调用 SAM3 三次
```

支持的拆分符：逗号（`,`）、分号（`;`）、`和`、`或`、`&`

### 提示词长度控制

- 最佳长度：2-4 个词，如 `"黄色校车"`、`"条纹红伞"`
- 安全上限：8 个词，超过此长度准确率下降
- 不要使用从句：`"那个昨天在操场上的穿红衣服的男孩"` → `"红衣服男孩"`

### 歧义处理

上下文消歧示例：

| 输入 | 上下文 | 优化提示词 |
|---|---|---|
| `"窗口"` | 安防监控 | `"建筑窗口"` |
| `"窗口"` | 医疗影像 | 需提示用户澄清 |
| `"苹果"` | 水果 | `"水果苹果"` |
| `"苹果"` | 电子产品 | `"手机"` |

---

## 第三部分：输出格式

### 统一检测格式

无论使用哪种后端（SAM3、Grounded SAM 2、YOLO-World），输出的概念检测结果应统一为以下格式：

```python
{
    "concept": "黄色校车",
    "instance_id": 0,
    "box": [x1, y1, x2, y2],
    "score": 0.92,
    "mask_rle": "0x100;1x50;0x200",  # 仅分割模型有此字段
}
```

### 多概念查询合并策略

1. 分别运行每个概念的检测
2. 合并到一个结果列表
3. 可选：执行 NMS 去重（不同概念间可能检测到同一实例）
4. 每个结果携带 `concept` 标签供下游区分

---

## 第四部分：生产部署建议

### 延迟优化

| 方法 | 预期加速 | 代价 |
|---|---|---|
| FP16 推理 | 1.5-2x | 轻微精度损失 |
| 批量提示词 | 取决于批量大小 | 增加首次词元时间 |
| 图像预处理缓存 | 避免重复处理 | 增加内存 |
| 实例跟踪分离 | 减少跟踪计算 | 增加工程复杂度 |

### 性能监控关键指标

- **每帧概念数**：提示词中拆出的概念数，直接影响延迟
- **存在性分数**：SAM3 存在性头部的输出，0-1，阈值 ≥ 0.5 为推荐值
- **掩码像素率**：RLE 编码长度监控，异常长编码可能表示质量下降
- **概念类型分布**：跟踪哪些概念被频繁查询，优化缓存策略

### 错误模式

| 错误现象 | 可能原因 | 解决方案 |
|---|---|---|
| 概念名称被忽略（空检测结果） | 概念过于抽象（"那些东西"） | 使用具体名词 |
| 检测出完全无关的对象 | 提示词存在歧义 | 添加上下文修饰词 |
| 部分实例漏检 | 概念在训练数据中覆盖率低 | 尝试 Grounded SAM 2 换检测器 |
| 掩码质量差，边缘粗糙 | 输入分辨率不足 | 提高输入图像分辨率 |
